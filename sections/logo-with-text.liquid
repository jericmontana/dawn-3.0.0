{{ 'section-logo-with-text-7923216.css' | asset_url | stylesheet_tag }}

{% assign use_as_default_padding = section.settings.use_as_default_padding %}
{% assign use_as_default_bg = section.settings.use_as_default_bg %}
{%- style -%}
    {%- if use_as_default_padding -%}
        .section--logo-with-text .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px !important;
            padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px !important;
        }

        @media screen and (min-width: 750px) {
            .section--logo-with-text .section-{{ section.id }}-padding {
            padding-top: {{ section.settings.padding_top }}px !important;
            padding-bottom: {{ section.settings.padding_bottom }}px !important;
            }
        }
    {%- endif -%}

    {%- if  use_as_default_bg -%}
        .section--logo-with-text .section-{{ section.id }}-bg-contaier {
            --color-foreground: {{ section.settings.fg_color.red }},{{ section.settings.fg_color.green }},{{ section.settings.fg_color.blue }};
            --color-background: {{ section.settings.bg_color.red }},{{ section.settings.bg_color.green }},{{ section.settings.bg_color.blue }};
            background-image: url('{{ section.settings.bg_image | image_url }}');
            background-color: {{ section.settings.bg_color }};
            background-blend-mode: {{ section.settings.bg_blend }};
        }
    {%- endif -%}

    .section--logo-with-text-{{ section.id }} .section__collection{
        border-radius: {{ section.settings.cc_border_radius }}rem;
        background-color: {{ section.settings.cc_bg_color }};
    }

    {% case section.settings.color_scheme %}
        {% when 'background-1' %}
        .section--logo-with-text-{{ section.id }} .section__header--title{
            color: rgb({{ settings.colors_accent_2.red }}, {{ settings.colors_accent_2.green }}, {{ settings.colors_accent_2.blue }});
        }

        .section--logo-with-text-{{ section.id }} .section__logo-and-text--text .h3{
            color: rgb({{ settings.colors_accent_2.red }}, {{ settings.colors_accent_2.green }}, {{ settings.colors_accent_2.blue }});
        }
    {% endcase %}
{%- endstyle -%}

{% capture grid_device_class %}
    grid--{{ section.settings.columns_mobile }}-col grid--{{ section.settings.columns_desktop }}-col-desktop grid--{{ section.settings.columns_tablet }}-col-tablet
{% endcapture %}

<div class="section section--logo-with-text section--logo-with-text-{{ section.id }} {% unless use_as_default_bg %}color-{{ section.settings.color_scheme }}{% endunless %}">
    <div class="section__bg-container section-{{ section.id }}-bg-contaier section-{{ section.id }}-padding">
        <div class="page-width">

            {% comment %} For Section Header Support {% endcomment %}
            {%- assign header_support = section.blocks | where: 'type', 'heading_support' -%}

            {% comment %} For Section Header {% endcomment %}
            {%- if header_support.size < 1 -%}
            {%- assign header = section.blocks | where: 'type', 'heading' -%}
            {%- for block in header -%}
                {%- if block.settings.title != blank or block.settings.subtitle != blank -%}
                {%- render 'section-header',
                    title: block.settings.title,
                    subtitle: block.settings.subtitle,
                    uppercase: block.settings.title_uppercase
                -%}
                {%- endif -%}
            {%- endfor -%}
            {%- endif -%}

            {% comment %} For Section Header Support {% endcomment %}
            {%- for block in header_support -%}
                {%- if block.settings.title_support != blank -%}
                <div class="section__header center">
                    <h2 class="section__header--title">{{ block.settings.title_support }}</h2>
                </div>
                {%- endif -%}
            {%- endfor -%}
            
            {% comment %} For Section Logo With Text {% endcomment %}
            {%- assign image_with_text_blocks = section.blocks | where: 'type', 'image_with_text' -%}
            {%- if image_with_text_blocks -%}
                <div class="section__logo-and-text">
                    <ul class="grid {{ grid_device_class }}">
                        {%- for image_with_text_block in image_with_text_blocks -%}
                            <li class="grid__item" title="{{ image_with_text_block.settings.logo_svg }}">
                                <div class="section__logo-and-text--logo">
                                {%- if image_with_text_block.settings.use_image -%}
                                    {%- if image_with_text_block.settings.image == blank -%}
                                        {% render 'no-image' %}
                                    {%- else -%}
                                        {%- liquid
                                            if image_with_text_block.settings.image.alt == blank
                                                assign image_alt = image_with_text_block.settings.title
                                                assign image_title = image_with_text_block.settings.title
                                            else
                                                assign image_alt = image_with_text_block.settings.image.alt
                                            endif
                                        -%}
                                        <img
                                            srcset="{%- if image_with_text_block.settings.image.width >= 165 -%}{{ image_with_text_block.settings.image | image_url: width: 165 }} 165w,{%- endif -%}
                                            {%- if image_with_text_block.settings.image.width >= 360 -%}{{ image_with_text_block.settings.image | image_url: width: 360 }} 360w,{%- endif -%}
                                            {%- if image_with_text_block.settings.image.width >= 533 -%}{{ image_with_text_block.settings.image | image_url: width: 533 }} 533w,{%- endif -%}
                                            {%- if image_with_text_block.settings.image.width >= 720 -%}{{ image_with_text_block.settings.image | image_url: width: 720 }} 720w,{%- endif -%}
                                            {%- if image_with_text_block.settings.image.width >= 940 -%}{{ image_with_text_block.settings.image | image_url: width: 940 }} 940w,{%- endif -%}
                                            {%- if image_with_text_block.settings.image.width >= 1066 -%}{{ image_with_text_block.settings.image | image_url: width: 1066 }} 1066w,{%- endif -%}
                                            {{ image_with_text_block.settings.image | image_url }} {{ image_with_text_block.settings.image.width }}w"
                                            src="{{ image_with_text_block.settings.image | image_url: width: 165 }}"
                                            sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 130 | divided_by: 4 }}px, (min-width: 990px) calc((100vw - 130px) / 4), (min-width: 750px) calc((100vw - 120px) / 3), calc((100vw - 35px) / 2)"
                                            alt="{{ image_alt | escape }}"
                                            title="{{ image_title | escape }}"
                                            class="motion-reduce"
                                            loading="lazy"
                                            width="{{ image_with_text_block.settings.image.width }}"
                                            height="{{ image_with_text_block.settings.image.height }}"
                                        >
                                    {%- endif -%}
                                {%- else -%}
                                    {%- render 'logo-svg', logo_svg: image_with_text_block.settings.logo_svg -%}
                                {%- endif -%}
                                </div>
                                <div class="section__logo-and-text--text">
                                    <h3 class="h3 center">{{ image_with_text_block.settings.title | escape }}</h3>
                                </div>
                            </li>
                        {%- endfor -%}
                    </ul>
                </div>
            {%- endif -%}

        </div>
    </div>
</div>

{% schema %}
{
  "name": "🔰 Logo with text",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Default Background"
    },
    {
      "type": "select",
      "id": "color_scheme",
      "options": [
        {
          "value": "accent-1",
          "label": "Accent 1"
        },
        {
          "value": "accent-2",
          "label": "Accent 2"
        },
        {
          "value": "background-1",
          "label": "Background 1"
        },
        {
          "value": "background-2",
          "label": "Background 2"
        },
        {
          "value": "inverse",
          "label": "Inverse"
        }
      ],
      "default": "background-1",
      "label": "Color Scheme",
      "info": "To change the card color scheme, update your theme settings."
    },
    {
      "type": "header",
      "content": "Custom Color Scheme"
    },
    {
      "type": "checkbox",
      "id": "use_as_default_bg",
      "label": "Set as default",
      "default": false
    },
    {
      "type": "color",
      "id": "fg_color",
      "label": "Foreground Color",
      "default": "#121212"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background Image"
    },
    {
      "type": "select",
      "id": "bg_blend",
      "label": "Blend Mode",
      "options": [
        {
          "value": "normal",
          "label": "Normal"
        },
        {
          "value": "multiply",
          "label": "Multiply"
        },
        {
          "value": "screen",
          "label": "Screen"
        },
        {
          "value": "overlay",
          "label": "Overlay"
        },
        {
          "value": "darken",
          "label": "Darken"
        },
        {
          "value": "lighten",
          "label": "Lighten"
        },
        {
          "value": "color-dodge",
          "label": "Color Dodge"
        },
        {
          "value": "saturation",
          "label": "Saturation"
        },
        {
          "value": "color",
          "label": "Color"
        },
        {
          "value": "luminosity",
          "label": "Luminosity"
        }
      ],
      "default": "normal"
    },
    {
      "type": "header",
      "content": "Grid Settings"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 3,
      "max": 8,
      "step": 1,
      "default": 4,
      "label": "Columns on Desktop",
      "info": "Default Value is 4"
    },
    {
      "type": "range",
      "id": "columns_tablet",
      "min": 2,
      "max": 4,
      "step": 1,
      "default": 4,
      "label": "Columns on Tablet",
      "info": "Default Value is 4"
    },
    {
      "type": "range",
      "id": "columns_mobile",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 1,
      "label": "Columns on Mobile",
      "info": "Default Value is 2"
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "checkbox",
      "id": "use_as_default_padding",
      "label": "Set as default",
      "default": false
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36,
      "info": "Default Value is 36"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36,
      "info": "Default Value is 36"
    }
  ],
  "blocks": [
    {
      "type": "heading",
      "name": "Header",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "Header Settings"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Why Choose Us"
        },
        {
          "type": "checkbox",
          "id": "title_uppercase",
          "label": "Uppercase",
          "default": false
        },
        {
          "type": "textarea",
          "id": "subtitle",
          "label": "Subtitle",
          "default": "Mus mauris vitae ultricies leo. Ac feugiat sed lectus vestibulum lacus laoreet non curabitur."
        }
      ]
    },
    {
      "type": "heading_support",
      "name": "Support Title",
      "limit": 1,
      "settings": [
        {
          "type": "richtext",
          "id": "title_support",
          "label": "Title Support",
          "default": "<p>Consectetur letius torquent nunc lacus volutpat</p>",
          "info": "Header Block will be disregarded."
        }
      ]
    },
    {
      "type": "image_with_text",
      "name": "Image with text",
      "limit": 8,
      "settings": [
        {
          "type": "select",
          "id": "logo_svg",
          "label": "SVG",
          "options": [
            {
              "value": "free-shipping",
              "label": "Free Shipping"
            },
            {
              "value": "high-quality",
              "label": "High Quality"
            },
            {
              "value": "loved-by-hundreds",
              "label": "Loved By Hundreds"
            },
            {
              "value": "substantial",
              "label": "Substantial"
            }
          ],
          "default": "loved-by-hundreds"
        },
        {
          "type": "checkbox",
          "id": "use_image",
          "label": "Use Image",
          "default": true
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Arcu rutrum"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "🔰 Logo with text",
      "blocks": [
        {
          "type": "heading"
        },
        {
          "type": "image_with_text"
        },
        {
          "type": "image_with_text"
        },
        {
          "type": "image_with_text"
        },
        {
          "type": "image_with_text"
        }
      ]
    }
  ]
}
{% endschema %}
